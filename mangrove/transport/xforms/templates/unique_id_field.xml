<select1 ref="/data/{{ field.code }}" appearance="quick">
        <label>{{ field.label }}</label>
        {% for (code, name) in field.options %}
            <item>
                <label>{{ name }} ({{ code }})</label>
                <value>{{ code }}</value>
            </item>
        {% endfor %}
</select1>